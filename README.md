# Nội dung
Bài tập buổi 8

# Cài đặt
```
yarn install
```
# Auto Init Models
```
sequelize-auto -h localhost -d ORM-DB -u [username] -x [password] -p 3306 --dialect mysql -o src/models -l esm
```